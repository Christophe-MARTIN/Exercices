tab=[1,2,3,4,5,10,20,30,40,50]
for(i=0 ; i<tab.length ; i++){
  console.log("A la case " + i +" le tableau prend la valeur " + tab[i])
  }
  for(i=0; i<tab.length ; i++){
  tab[i] = tab[i]*10
  console.log("A la case " + i + " le tableau prend la valeur " + tab[i])
  document.write("A la case " + i + " le tableau prend la valeur " tab[i])
  }
